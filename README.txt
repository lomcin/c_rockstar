The task here is to find the optimal force applied to a point mass which
minimizes a given cost function. The cost function penalizes error in final
distance and velocity plus the sum of square force applied during the task.

Run CMA-ES too for comparison